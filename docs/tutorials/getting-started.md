# Getting started with qcom-build-utils

This tutorial was generated by the Hosted Docs DocsGen pipeline from the public fork of Qualcomm Linux `qcom-build-utils`.

## What this repo is

`qcom-build-utils` holds helper scripts around Qualcomm Linux image builds. The fork used here is `devdocsorg/qli-qcom-build-utils`.

## Typical workflow

1. Source the environment the README describes.
2. Run the wrapper that matches your host (Linux CI or workstation).
3. Collect the artifacts the script prints at the end.

## Next

The DocsGen queue writes more tutorials here one page at a time. Hosted Docs keeps those pages in Draft reviews until an operator approves them.
